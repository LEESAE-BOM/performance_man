import 'package:flutter/material.dart';


class management_main extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Container(
            child: Center(
                child: Text('경영 메인')
            )
        )
    );
  }
}