import 'package:flutter/material.dart';


class lead_time extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Container(
            child: Center(
                child: Text('리드타임')
            )
        )
    );
  }
}