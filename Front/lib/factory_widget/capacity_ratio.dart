import 'package:flutter/material.dart';


class capacity_ratio extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Container(
            child: Center(
                child: Text('설비가동율')
            )
        )
    );
  }
}